# 依赖
node_modules

# 构建产物 (vite)
dist
dist-electron
.vite

# 打包产物 (Electron Builder)
release

# 本地环境变量
.env.local
.env.*.local

# 编辑器 / 本地工具配置
.claude/settings.local.json
.vscode/*
.idea

# 应用以本仓库为工作目录运行时产生的项目级配置/资源目录
.oint/

# 日志 / 系统文件
*.log
# 运行时日志目录（当前代码未使用，保留目录但别让它被提交）
logs/
.DS_Store
Thumbs.db
