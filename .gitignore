# 依赖
node_modules

# 构建产物 (vite)
dist
dist-electron
.vite

# 打包产物 (Electron Builder)
release

# 本地环境变量
.env.local
.env.*.local

# 编辑器 / 本地工具配置
.claude/settings.local.json
.vscode/*
.idea

# 日志 / 系统文件
*.log
# 运行时日志目录（当前代码未使用，保留目录但别让它被提交）
logs/
.DS_Store
Thumbs.db
